---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "openwebui_rag_config Resource - openwebui"
subcategory: ""
description: |-
  Manages Open WebUI's retrieval settings: document loaders, chunking, reranking, file limits, and web search.
  The top-level keys keep their stored value when a request leaves them out, but every key of the web block is written on every write. This resource therefore reads the current configuration and overlays the attributes the plan names, so a partial configuration never clears a key it does not mention.
  POST /api/v1/retrieval/config/update answers with a response that omits five of the keys the GET returns, so this resource reads the configuration back after every write.
  Four keys are loaded but unreachable through this router: AZURE_AI_SEARCH_API_KEY, AZURE_AI_SEARCH_ENDPOINT, AZURE_AI_SEARCH_INDEX_NAME, and TIKTOKEN_ENCODING_NAME. Set them with openwebui_config_import.
  YOUTUBE_LOADER_TRANSLATION has no attribute here. It is in WebConfig but not in RETRIEVAL_CONFIG_KEYS, so Open WebUI holds it in process memory and loses it on restart.
---

# openwebui_rag_config (Resource)

Manages Open WebUI's retrieval settings: document loaders, chunking, reranking, file limits, and web search.

The top-level keys keep their stored value when a request leaves them out, but every key of the `web` block is written on every write. This resource therefore reads the current configuration and overlays the attributes the plan names, so a partial configuration never clears a key it does not mention.

`POST /api/v1/retrieval/config/update` answers with a response that omits five of the keys the GET returns, so this resource reads the configuration back after every write.

Four keys are loaded but unreachable through this router: `AZURE_AI_SEARCH_API_KEY`, `AZURE_AI_SEARCH_ENDPOINT`, `AZURE_AI_SEARCH_INDEX_NAME`, and `TIKTOKEN_ENCODING_NAME`. Set them with `openwebui_config_import`.

`YOUTUBE_LOADER_TRANSLATION` has no attribute here. It is in `WebConfig` but not in `RETRIEVAL_CONFIG_KEYS`, so Open WebUI holds it in process memory and loses it on restart.

## Example Usage

```terraform
# Every attribute this resource does not name keeps the value Open WebUI already
# holds. The web block is the exception: Open WebUI writes all of its keys on
# every write, so the resource reads them back and sends them whole.
resource "openwebui_rag_config" "example" {
  chunk_size    = 1000
  chunk_overlap = 100
  top_k         = 4

  # The empty string removes the limit.
  file_max_size  = "50"
  file_max_count = ""

  web = {
    enable_web_search       = true
    web_search_engine       = "tavily"
    web_search_result_count = 5
    tavily_api_key          = var.tavily_api_key
  }
}

variable "tavily_api_key" {
  type      = string
  sensitive = true
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `allowed_file_extensions` (List of String) Open WebUI setting `ALLOWED_FILE_EXTENSIONS`. Stored as `rag.file.allowed_extensions`.
- `bypass_embedding_and_retrieval` (Boolean) Open WebUI setting `BYPASS_EMBEDDING_AND_RETRIEVAL`. Stored as `rag.bypass_embedding_and_retrieval`.
- `chunk_min_size_target` (Number) Open WebUI setting `CHUNK_MIN_SIZE_TARGET`. Stored as `rag.chunk_min_size_target`.
- `chunk_overlap` (Number) Open WebUI setting `CHUNK_OVERLAP`. Stored as `rag.chunk_overlap`.
- `chunk_size` (Number) Open WebUI setting `CHUNK_SIZE`. Stored as `rag.chunk_size`.
- `content_extraction_engine` (String) Open WebUI setting `CONTENT_EXTRACTION_ENGINE`. Stored as `rag.content_extraction_engine`.
- `content_extraction_supported_media_mime_types` (List of String) Open WebUI setting `CONTENT_EXTRACTION_SUPPORTED_MEDIA_MIME_TYPES`. Stored as `rag.content_extraction.supported_media_mime_types`.
- `datalab_marker_additional_config` (String) Open WebUI setting `DATALAB_MARKER_ADDITIONAL_CONFIG`. Stored as `rag.datalab_marker_additional_config`.
- `datalab_marker_api_base_url` (String) Open WebUI setting `DATALAB_MARKER_API_BASE_URL`. Stored as `rag.datalab_marker_api_base_url`.
- `datalab_marker_api_key` (String, Sensitive) Open WebUI setting `DATALAB_MARKER_API_KEY`. Stored as `rag.datalab_marker_api_key`. Sensitive.
- `datalab_marker_disable_image_extraction` (Boolean) Open WebUI setting `DATALAB_MARKER_DISABLE_IMAGE_EXTRACTION`. Stored as `rag.datalab_marker_disable_image_extraction`.
- `datalab_marker_force_ocr` (Boolean) Open WebUI setting `DATALAB_MARKER_FORCE_OCR`. Stored as `rag.datalab_marker_force_ocr`.
- `datalab_marker_format_lines` (Boolean) Open WebUI setting `DATALAB_MARKER_FORMAT_LINES`. Stored as `rag.datalab_marker_format_lines`.
- `datalab_marker_output_format` (String) Open WebUI setting `DATALAB_MARKER_OUTPUT_FORMAT`. Stored as `rag.datalab_marker_output_format`.
- `datalab_marker_paginate` (Boolean) Open WebUI setting `DATALAB_MARKER_PAGINATE`. Stored as `rag.datalab_marker_paginate`.
- `datalab_marker_skip_cache` (Boolean) Open WebUI setting `DATALAB_MARKER_SKIP_CACHE`. Stored as `rag.datalab_marker_skip_cache`.
- `datalab_marker_strip_existing_ocr` (Boolean) Open WebUI setting `DATALAB_MARKER_STRIP_EXISTING_OCR`. Stored as `rag.datalab_marker_strip_existing_ocr`.
- `datalab_marker_use_llm` (Boolean) Open WebUI setting `DATALAB_MARKER_USE_LLM`. Stored as `rag.datalab_marker_use_llm`.
- `docling_api_key` (String, Sensitive) Open WebUI setting `DOCLING_API_KEY`. Stored as `rag.docling_api_key`. Sensitive.
- `docling_params` (String) JSON object of extra parameters passed to Docling. Stored as `rag.docling_params`.
- `docling_server_url` (String) Open WebUI setting `DOCLING_SERVER_URL`. Stored as `rag.docling_server_url`.
- `document_intelligence_endpoint` (String) Open WebUI setting `DOCUMENT_INTELLIGENCE_ENDPOINT`. Stored as `rag.document_intelligence_endpoint`.
- `document_intelligence_key` (String, Sensitive) Open WebUI setting `DOCUMENT_INTELLIGENCE_KEY`. Stored as `rag.document_intelligence_key`. Sensitive.
- `document_intelligence_model` (String) Open WebUI setting `DOCUMENT_INTELLIGENCE_MODEL`. Stored as `rag.document_intelligence_model`.
- `enable_google_drive_integration` (Boolean) Open WebUI setting `ENABLE_GOOGLE_DRIVE_INTEGRATION`. Stored as `google_drive.enable`.
- `enable_markdown_header_text_splitter` (Boolean) Open WebUI setting `ENABLE_MARKDOWN_HEADER_TEXT_SPLITTER`. Stored as `rag.enable_markdown_header_text_splitter`.
- `enable_onedrive_integration` (Boolean) Open WebUI setting `ENABLE_ONEDRIVE_INTEGRATION`. Stored as `onedrive.enable`.
- `enable_rag_hybrid_search` (Boolean) Open WebUI setting `ENABLE_RAG_HYBRID_SEARCH`. Stored as `rag.enable_hybrid_search`.
- `enable_rag_hybrid_search_enriched_texts` (Boolean) Open WebUI setting `ENABLE_RAG_HYBRID_SEARCH_ENRICHED_TEXTS`. Stored as `rag.enable_hybrid_search_enriched_texts`.
- `external_document_loader_api_key` (String, Sensitive) Open WebUI setting `EXTERNAL_DOCUMENT_LOADER_API_KEY`. Stored as `rag.external_document_loader_api_key`. Sensitive.
- `external_document_loader_headers` (String, Sensitive) JSON object of headers sent to the external document loader. Carries the loader's credentials. Stored as `rag.external_document_loader_headers`. Sensitive.
- `external_document_loader_url` (String) Open WebUI setting `EXTERNAL_DOCUMENT_LOADER_URL`. Stored as `rag.external_document_loader_url`.
- `file_image_compression_height` (String) Height an uploaded image is compressed to. The empty string turns compression off. Stored as `file.image_compression_height`.
- `file_image_compression_width` (String) Width an uploaded image is compressed to. The empty string turns compression off. Stored as `file.image_compression_width`.
- `file_max_count` (String) Largest number of files one chat message may carry. The empty string removes the limit. Stored as `rag.file.max_count`.
- `file_max_size` (String) Largest upload Open WebUI accepts, in megabytes. The empty string removes the limit. Stored as `rag.file.max_size`.
- `hybrid_bm25_weight` (Number) Open WebUI setting `HYBRID_BM25_WEIGHT`. Stored as `rag.hybrid_bm25_weight`.
- `mineru_api_key` (String, Sensitive) Open WebUI setting `MINERU_API_KEY`. Stored as `rag.mineru_api_key`. Sensitive.
- `mineru_api_mode` (String) Open WebUI setting `MINERU_API_MODE`. Stored as `rag.mineru_api_mode`.
- `mineru_api_timeout` (Number) MinerU request timeout in seconds. Open WebUI ships the default as a string and reads it back as either a string or a number. Stored as `rag.mineru_api_timeout`.
- `mineru_api_url` (String) Open WebUI setting `MINERU_API_URL`. Stored as `rag.mineru_api_url`.
- `mineru_file_extensions` (List of String) Open WebUI setting `MINERU_FILE_EXTENSIONS`. Stored as `rag.mineru_file_extensions`.
- `mineru_params` (String) JSON object of extra parameters passed to MinerU. Stored as `rag.mineru_params`.
- `mistral_ocr_api_base_url` (String) Open WebUI setting `MISTRAL_OCR_API_BASE_URL`. Stored as `rag.mistral_ocr_api_base_url`.
- `mistral_ocr_api_key` (String, Sensitive) Open WebUI setting `MISTRAL_OCR_API_KEY`. Stored as `rag.mistral_ocr_api_key`. Sensitive.
- `mistral_ocr_use_base64` (Boolean) Open WebUI setting `MISTRAL_OCR_USE_BASE64`. Stored as `rag.mistral_ocr_use_base64`.
- `paddleocr_vl_base_url` (String) Open WebUI setting `PADDLEOCR_VL_BASE_URL`. Stored as `rag.paddleocr_vl_base_url`.
- `paddleocr_vl_token` (String, Sensitive) Open WebUI setting `PADDLEOCR_VL_TOKEN`. Stored as `rag.paddleocr_vl_token`. Sensitive.
- `pdf_extract_images` (Boolean) Open WebUI setting `PDF_EXTRACT_IMAGES`. Stored as `rag.pdf_extract_images`.
- `pdf_loader_mode` (String) Open WebUI setting `PDF_LOADER_MODE`. Stored as `rag.pdf_loader_mode`.
- `rag_external_reranker_api_key` (String, Sensitive) Open WebUI setting `RAG_EXTERNAL_RERANKER_API_KEY`. Stored as `rag.external_reranker_api_key`. Sensitive.
- `rag_external_reranker_timeout` (String) Open WebUI setting `RAG_EXTERNAL_RERANKER_TIMEOUT`. Stored as `rag.external_reranker_timeout`.
- `rag_external_reranker_url` (String) Open WebUI setting `RAG_EXTERNAL_RERANKER_URL`. Stored as `rag.external_reranker_url`.
- `rag_full_context` (Boolean) Open WebUI setting `RAG_FULL_CONTEXT`. Stored as `rag.full_context`.
- `rag_reranking_batch_size` (Number) Open WebUI setting `RAG_RERANKING_BATCH_SIZE`. Stored as `rag.reranking_batch_size`.
- `rag_reranking_engine` (String) Open WebUI setting `RAG_RERANKING_ENGINE`. Stored as `rag.reranking_engine`.
- `rag_reranking_model` (String) Open WebUI setting `RAG_RERANKING_MODEL`. Stored as `rag.reranking_model`.
- `rag_template` (String) Prompt template that wraps retrieved context. Stored as `rag.template`.
- `rag_tokenizer_model` (String) Open WebUI setting `RAG_TOKENIZER_MODEL`. Stored as `rag.tokenizer_model`.
- `relevance_threshold` (Number) Open WebUI setting `RELEVANCE_THRESHOLD`. Stored as `rag.relevance_threshold`.
- `text_splitter` (String) Open WebUI setting `TEXT_SPLITTER`. Stored as `rag.text_splitter`.
- `tika_server_url` (String) Open WebUI setting `TIKA_SERVER_URL`. Stored as `rag.tika_server_url`.
- `top_k` (Number) Open WebUI setting `TOP_K`. Stored as `rag.top_k`.
- `top_k_reranker` (Number) Open WebUI setting `TOP_K_RERANKER`. Stored as `rag.top_k_reranker`.
- `web` (Attributes) Web search and web loader settings. Open WebUI writes every key of this block on every write. (see [below for nested schema](#nestedatt--web))

### Read-Only

- `id` (String) Identifier of this singleton resource. Always `rag`.

<a id="nestedatt--web"></a>
### Nested Schema for `web`

Optional:

- `bing_search_v7_endpoint` (String) Open WebUI setting `BING_SEARCH_V7_ENDPOINT`. Stored as `web.search.bing_search_v7_endpoint`.
- `bing_search_v7_subscription_key` (String, Sensitive) Open WebUI setting `BING_SEARCH_V7_SUBSCRIPTION_KEY`. Stored as `web.search.bing_search_v7_subscription_key`. Sensitive.
- `bocha_search_api_key` (String, Sensitive) Open WebUI setting `BOCHA_SEARCH_API_KEY`. Stored as `web.search.bocha_search_api_key`. Sensitive.
- `brave_search_api_key` (String, Sensitive) Open WebUI setting `BRAVE_SEARCH_API_KEY`. Stored as `web.search.brave_search_api_key`. Sensitive.
- `brave_search_context_tokens` (Number) Open WebUI setting `BRAVE_SEARCH_CONTEXT_TOKENS`. Stored as `web.search.brave_search_context_tokens`.
- `bypass_web_search_embedding_and_retrieval` (Boolean) Open WebUI setting `BYPASS_WEB_SEARCH_EMBEDDING_AND_RETRIEVAL`. Stored as `web.search.bypass_embedding_and_retrieval`.
- `bypass_web_search_web_loader` (Boolean) Open WebUI setting `BYPASS_WEB_SEARCH_WEB_LOADER`. Stored as `web.search.bypass_web_loader`.
- `ddgs_backend` (String) Open WebUI setting `DDGS_BACKEND`. Stored as `web.search.ddgs_backend`.
- `enable_web_loader_ssl_verification` (Boolean) Open WebUI setting `ENABLE_WEB_LOADER_SSL_VERIFICATION`. Stored as `web.loader.ssl_verification`.
- `enable_web_search` (Boolean) Open WebUI setting `ENABLE_WEB_SEARCH`. Stored as `web.search.enable`.
- `enable_web_search_confirmation` (Boolean) Open WebUI setting `ENABLE_WEB_SEARCH_CONFIRMATION`. Stored as `web.search.confirmation.enable`.
- `exa_api_key` (String, Sensitive) Open WebUI setting `EXA_API_KEY`. Stored as `web.search.exa_api_key`. Sensitive.
- `external_web_loader_api_key` (String, Sensitive) Open WebUI setting `EXTERNAL_WEB_LOADER_API_KEY`. Stored as `web.loader.external_web_loader_api_key`. Sensitive.
- `external_web_loader_url` (String) Open WebUI setting `EXTERNAL_WEB_LOADER_URL`. Stored as `web.loader.external_web_loader_url`.
- `external_web_search_api_key` (String, Sensitive) Open WebUI setting `EXTERNAL_WEB_SEARCH_API_KEY`. Stored as `web.search.external_web_search_api_key`. Sensitive.
- `external_web_search_url` (String) Open WebUI setting `EXTERNAL_WEB_SEARCH_URL`. Stored as `web.search.external_web_search_url`.
- `firecrawl_api_base_url` (String) Open WebUI setting `FIRECRAWL_API_BASE_URL`. Stored as `web.loader.firecrawl_api_url`.
- `firecrawl_api_key` (String, Sensitive) Open WebUI setting `FIRECRAWL_API_KEY`. Stored as `web.loader.firecrawl_api_key`. Sensitive.
- `firecrawl_timeout` (String) Open WebUI setting `FIRECRAWL_TIMEOUT`. Stored as `web.loader.firecrawl_timeout`.
- `google_pse_api_key` (String, Sensitive) Open WebUI setting `GOOGLE_PSE_API_KEY`. Stored as `web.search.google_pse_api_key`. Sensitive.
- `google_pse_engine_id` (String) Open WebUI setting `GOOGLE_PSE_ENGINE_ID`. Stored as `web.search.google_pse_engine_id`.
- `jina_api_base_url` (String) Open WebUI setting `JINA_API_BASE_URL`. Stored as `web.search.jina_api_base_url`.
- `jina_api_key` (String, Sensitive) Open WebUI setting `JINA_API_KEY`. Stored as `web.search.jina_api_key`. Sensitive.
- `kagi_search_api_key` (String, Sensitive) Open WebUI setting `KAGI_SEARCH_API_KEY`. Stored as `web.search.kagi_search_api_key`. Sensitive.
- `linkup_api_key` (String, Sensitive) Open WebUI setting `LINKUP_API_KEY`. Stored as `web.search.linkup_api_key`. Sensitive.
- `linkup_search_params` (String) JSON object of extra parameters passed to Linkup. Stored as `web.search.linkup_search_params`.
- `microsoft_web_iq_api_base_url` (String) Open WebUI setting `MICROSOFT_WEB_IQ_API_BASE_URL`. Stored as `web.search.microsoft_web_iq_api_base_url`.
- `microsoft_web_iq_api_key` (String, Sensitive) Open WebUI setting `MICROSOFT_WEB_IQ_API_KEY`. Stored as `web.search.microsoft_web_iq_api_key`. Sensitive.
- `microsoft_web_iq_language` (String) Open WebUI setting `MICROSOFT_WEB_IQ_LANGUAGE`. Stored as `web.search.microsoft_web_iq_language`.
- `mojeek_search_api_key` (String, Sensitive) Open WebUI setting `MOJEEK_SEARCH_API_KEY`. Stored as `web.search.mojeek_search_api_key`. Sensitive.
- `ollama_cloud_web_search_api_key` (String, Sensitive) Open WebUI setting `OLLAMA_CLOUD_WEB_SEARCH_API_KEY`. Stored as `web.search.ollama_cloud_api_key`. Sensitive.
- `openserp_base_url` (String) Open WebUI setting `OPENSERP_BASE_URL`. Stored as `web.search.openserp_base_url`.
- `perplexity_api_key` (String, Sensitive) Open WebUI setting `PERPLEXITY_API_KEY`. Stored as `web.search.perplexity_api_key`. Sensitive.
- `perplexity_model` (String) Open WebUI setting `PERPLEXITY_MODEL`. Stored as `web.search.perplexity_model`.
- `perplexity_search_api_url` (String) Open WebUI setting `PERPLEXITY_SEARCH_API_URL`. Stored as `web.search.perplexity_search_api_url`.
- `perplexity_search_context_usage` (String) Open WebUI setting `PERPLEXITY_SEARCH_CONTEXT_USAGE`. Stored as `web.search.perplexity_search_context_usage`.
- `playwright_timeout` (Number) Open WebUI setting `PLAYWRIGHT_TIMEOUT`. Stored as `web.loader.playwright_timeout`.
- `playwright_ws_url` (String) Open WebUI setting `PLAYWRIGHT_WS_URL`. Stored as `web.loader.playwright_ws_url`.
- `searchapi_api_key` (String, Sensitive) Open WebUI setting `SEARCHAPI_API_KEY`. Stored as `web.search.searchapi_api_key`. Sensitive.
- `searchapi_engine` (String) Open WebUI setting `SEARCHAPI_ENGINE`. Stored as `web.search.searchapi_engine`.
- `searxng_language` (String) Open WebUI setting `SEARXNG_LANGUAGE`. Stored as `web.search.searxng_language`.
- `searxng_query_url` (String) Open WebUI setting `SEARXNG_QUERY_URL`. Stored as `web.search.searxng_query_url`.
- `serpapi_api_key` (String, Sensitive) Open WebUI setting `SERPAPI_API_KEY`. Stored as `web.search.serpapi_api_key`. Sensitive.
- `serpapi_engine` (String) Open WebUI setting `SERPAPI_ENGINE`. Stored as `web.search.serpapi_engine`.
- `serper_api_key` (String, Sensitive) Open WebUI setting `SERPER_API_KEY`. Stored as `web.search.serper_api_key`. Sensitive.
- `serphouse_api_key` (String, Sensitive) Open WebUI setting `SERPHOUSE_API_KEY`. Stored as `web.search.serphouse_api_key`. Sensitive.
- `serphouse_domain` (String) Open WebUI setting `SERPHOUSE_DOMAIN`. Stored as `web.search.serphouse_domain`.
- `serply_api_key` (String, Sensitive) Open WebUI setting `SERPLY_API_KEY`. Stored as `web.search.serply_api_key`. Sensitive.
- `serpstack_api_key` (String, Sensitive) Open WebUI setting `SERPSTACK_API_KEY`. Stored as `web.search.serpstack_api_key`. Sensitive.
- `serpstack_https` (Boolean) Open WebUI setting `SERPSTACK_HTTPS`. Stored as `web.search.serpstack_https`.
- `sougou_api_sid` (String) Open WebUI setting `SOUGOU_API_SID`. Stored as `web.search.sougou_api_sid`.
- `sougou_api_sk` (String, Sensitive) Open WebUI setting `SOUGOU_API_SK`. Stored as `web.search.sougou_api_sk`. Sensitive.
- `tavily_api_key` (String, Sensitive) Open WebUI setting `TAVILY_API_KEY`. Stored as `web.search.tavily_api_key`. Sensitive.
- `tavily_extract_depth` (String) Open WebUI setting `TAVILY_EXTRACT_DEPTH`. Stored as `web.search.tavily_extract_depth`.
- `web_fetch_max_content_length` (Number) Open WebUI setting `WEB_FETCH_MAX_CONTENT_LENGTH`. Stored as `web.fetch.max_content_length`.
- `web_loader_concurrent_requests` (Number) Open WebUI setting `WEB_LOADER_CONCURRENT_REQUESTS`. Stored as `web.loader.concurrent_requests`.
- `web_loader_engine` (String) Open WebUI setting `WEB_LOADER_ENGINE`. Stored as `web.loader.engine`.
- `web_loader_timeout` (String) Open WebUI setting `WEB_LOADER_TIMEOUT`. Stored as `web.loader.timeout`.
- `web_search_concurrent_requests` (Number) Open WebUI setting `WEB_SEARCH_CONCURRENT_REQUESTS`. Stored as `web.search.concurrent_requests`.
- `web_search_confirmation_content` (String) Open WebUI setting `WEB_SEARCH_CONFIRMATION_CONTENT`. Stored as `web.search.confirmation.content`.
- `web_search_domain_filter_list` (List of String) Domains web search results are restricted to. Stored as `web.search.domain.filter_list`.
- `web_search_engine` (String) Open WebUI setting `WEB_SEARCH_ENGINE`. Stored as `web.search.engine`.
- `web_search_result_count` (Number) Open WebUI setting `WEB_SEARCH_RESULT_COUNT`. Stored as `web.search.result_count`.
- `web_search_trust_env` (Boolean) Open WebUI setting `WEB_SEARCH_TRUST_ENV`. Stored as `web.search.trust_env`.
- `yacy_password` (String, Sensitive) Open WebUI setting `YACY_PASSWORD`. Stored as `web.search.yacy_password`. Sensitive.
- `yacy_query_url` (String) Open WebUI setting `YACY_QUERY_URL`. Stored as `web.search.yacy_query_url`.
- `yacy_username` (String) Open WebUI setting `YACY_USERNAME`. Stored as `web.search.yacy_username`.
- `yandex_web_search_api_key` (String, Sensitive) Open WebUI setting `YANDEX_WEB_SEARCH_API_KEY`. Stored as `web.search.yandex_web_search_api_key`. Sensitive.
- `yandex_web_search_config` (String) Open WebUI setting `YANDEX_WEB_SEARCH_CONFIG`. Stored as `web.search.yandex_web_search_config`.
- `yandex_web_search_url` (String) Open WebUI setting `YANDEX_WEB_SEARCH_URL`. Stored as `web.search.yandex_web_search_url`.
- `youcom_api_key` (String, Sensitive) Open WebUI setting `YOUCOM_API_KEY`. Stored as `web.search.youcom_api_key`. Sensitive.
- `youtube_loader_language` (List of String) Open WebUI setting `YOUTUBE_LOADER_LANGUAGE`. Stored as `rag.youtube_loader_language`.
- `youtube_loader_proxy_url` (String) Open WebUI setting `YOUTUBE_LOADER_PROXY_URL`. Stored as `rag.youtube_loader_proxy_url`.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Open WebUI holds one RAG configuration, so this resource is a singleton.
# Import it under its fixed id.
terraform import openwebui_rag_config.example rag
```
