---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "openwebui_tool_server Data Source - openwebui"
subcategory: ""
description: |-
  Reads one registered tool server by its server_id.
  The encrypted OAuth registration and the static client secret are not exposed here. Read them from the openwebui_tool_server resource that manages the connection.
---

# openwebui_tool_server (Data Source)

Reads one registered tool server by its `server_id`.

The encrypted OAuth registration and the static client secret are not exposed here. Read them from the `openwebui_tool_server` resource that manages the connection.

## Example Usage

```terraform
data "openwebui_tool_server" "paperless" {
  server_id = "paperless"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `server_id` (String) Identifier of the tool server, stored as `info.id`.

### Read-Only

- `auth_type` (String) How Open WebUI authenticates to the server.
- `description` (String) Description of an MCP server.
- `enabled` (Boolean) Whether Open WebUI loads the server.
- `headers_json` (String) JSON object of extra HTTP headers.
- `id` (String) Terraform identifier. Always equal to `server_id`.
- `key` (String, Sensitive) Bearer token sent to the tool server. Sensitive.
- `name` (String) Display name of an MCP server.
- `oauth_client_id` (String) OAuth client identifier used by a static OAuth connection.
- `oauth_resource_parameter` (String) How Open WebUI sends the OAuth resource indicator.
- `oauth_scope` (String) Space-separated OAuth scopes requested for this server.
- `path` (String) Path to the OpenAPI spec.
- `public_read` (Boolean) Whether every signed-in user can use the server's tools.
- `public_write` (Boolean) Whether every signed-in user can manage the server's tools.
- `read_groups` (List of String) Group names granted read access to the server's tools.
- `spec_type` (String) Where an OpenAPI server's spec comes from.
- `type` (String) Tool server type, `openapi` or `mcp`.
- `url` (String) Base URL of the tool server.
- `write_groups` (List of String) Group names granted write access to the server's tools.
